---
title: 'SDK di ASP.NET Core :::no-loc(Razor):::'
author: Rick-Anderson
description: "Informazioni :::no-loc(Razor)::: sul modo in cui le pagine ASP.NET Core rendono più semplici e più produttivi gli scenari incentrati sulla pagina della codifica rispetto all'utilizzo di MVC"
monikerRange: '>= aspnetcore-2.1'
ms.author: riande
ms.custom: mvc, seodec18
ms.date: 03/26/2020
no-loc:
- ':::no-loc(appsettings.json):::'
- ':::no-loc(ASP.NET Core Identity):::'
- ':::no-loc(cookie):::'
- ':::no-loc(Cookie):::'
- ':::no-loc(Blazor):::'
- ':::no-loc(Blazor Server):::'
- ':::no-loc(Blazor WebAssembly):::'
- ':::no-loc(Identity):::'
- ":::no-loc(Let's Encrypt):::"
- ':::no-loc(Razor):::'
- ':::no-loc(SignalR):::'
uid: razor-pages/sdk
ms.openlocfilehash: d3b01889b7634dce8ef1d6a4886a9a6ac39a6473
ms.sourcegitcommit: ca34c1ac578e7d3daa0febf1810ba5fc74f60bbf
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 10/30/2020
ms.locfileid: "93060872"
---
# <a name="aspnet-core-no-locrazor-sdk"></a><span data-ttu-id="e9fa6-103">SDK di ASP.NET Core :::no-loc(Razor):::</span><span class="sxs-lookup"><span data-stu-id="e9fa6-103">ASP.NET Core :::no-loc(Razor)::: SDK</span></span>

<span data-ttu-id="e9fa6-104">Autore: [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="e9fa6-104">By [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

## <a name="overview"></a><span data-ttu-id="e9fa6-105">Panoramica</span><span class="sxs-lookup"><span data-stu-id="e9fa6-105">Overview</span></span>

<span data-ttu-id="e9fa6-106">[!INCLUDE[](~/includes/2.1-SDK.md)]Include l' `Microsoft.NET.Sdk.:::no-loc(Razor):::` SDK di MSBuild ( :::no-loc(Razor)::: SDK).</span><span class="sxs-lookup"><span data-stu-id="e9fa6-106">The [!INCLUDE[](~/includes/2.1-SDK.md)] includes the `Microsoft.NET.Sdk.:::no-loc(Razor):::` MSBuild SDK (:::no-loc(Razor)::: SDK).</span></span> <span data-ttu-id="e9fa6-107">:::no-loc(Razor):::SDK:</span><span class="sxs-lookup"><span data-stu-id="e9fa6-107">The :::no-loc(Razor)::: SDK:</span></span>

::: moniker range=">= aspnetcore-3.0"

* <span data-ttu-id="e9fa6-108">È necessario per compilare, creare pacchetti e pubblicare progetti contenenti [:::no-loc(Razor):::](xref:mvc/views/razor) file per ASP.NET Core progetti basati su MVC o [:::no-loc(Blazor):::](xref:blazor/index) .</span><span class="sxs-lookup"><span data-stu-id="e9fa6-108">Is required to build, package, and publish projects containing [:::no-loc(Razor):::](xref:mvc/views/razor) files for ASP.NET Core MVC-based or [:::no-loc(Blazor):::](xref:blazor/index) projects.</span></span>
* <span data-ttu-id="e9fa6-109">Include un set di destinazioni, proprietà ed elementi predefiniti che consentono di personalizzare la compilazione di :::no-loc(Razor)::: file ( *. cshtml* o *. Razor* ).</span><span class="sxs-lookup"><span data-stu-id="e9fa6-109">Includes a set of predefined targets, properties, and items that allow customizing the compilation of :::no-loc(Razor)::: ( *.cshtml* or *.razor* ) files.</span></span>

<span data-ttu-id="e9fa6-110">L' :::no-loc(Razor)::: SDK include `Content` gli elementi con `Include` attributi impostati sui `**\*.cshtml` `**\*.razor` modelli glob e.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-110">The :::no-loc(Razor)::: SDK includes `Content` items with `Include` attributes set to the `**\*.cshtml` and `**\*.razor` globbing patterns.</span></span> <span data-ttu-id="e9fa6-111">I file corrispondenti vengono pubblicati.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-111">Matching files are published.</span></span>

::: moniker-end

::: moniker range="< aspnetcore-3.0"

* <span data-ttu-id="e9fa6-112">Standardizza l'esperienza di compilazione, creazione di pacchetti e pubblicazione di progetti contenenti [:::no-loc(Razor):::](xref:mvc/views/razor) file per ASP.NET Core progetti basati su MVC.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-112">Standardizes the experience around building, packaging, and publishing projects containing [:::no-loc(Razor):::](xref:mvc/views/razor) files for ASP.NET Core MVC-based projects.</span></span>
* <span data-ttu-id="e9fa6-113">Include un set di destinazioni, proprietà ed elementi predefiniti che consentono di personalizzare la compilazione dei :::no-loc(Razor)::: file.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-113">Includes a set of predefined targets, properties, and items that allow customizing the compilation of :::no-loc(Razor)::: files.</span></span>

<span data-ttu-id="e9fa6-114">L' :::no-loc(Razor)::: SDK include un `Content` elemento con un `Include` attributo impostato sul `**\*.cshtml` modello glob.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-114">The :::no-loc(Razor)::: SDK includes a `Content` item with an `Include` attribute set to the `**\*.cshtml` globbing pattern.</span></span> <span data-ttu-id="e9fa6-115">I file corrispondenti vengono pubblicati.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-115">Matching files are published.</span></span>

::: moniker-end

## <a name="prerequisites"></a><span data-ttu-id="e9fa6-116">Prerequisiti</span><span class="sxs-lookup"><span data-stu-id="e9fa6-116">Prerequisites</span></span>

[!INCLUDE[](~/includes/2.1-SDK.md)]

## <a name="use-the-no-locrazor-sdk"></a><span data-ttu-id="e9fa6-117">Usare l' :::no-loc(Razor)::: SDK</span><span class="sxs-lookup"><span data-stu-id="e9fa6-117">Use the :::no-loc(Razor)::: SDK</span></span>

<span data-ttu-id="e9fa6-118">La maggior parte delle app Web non è necessaria per fare riferimento in modo esplicito all' :::no-loc(Razor)::: SDK.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-118">Most web apps aren't required to explicitly reference the :::no-loc(Razor)::: SDK.</span></span>

::: moniker range=">= aspnetcore-3.0"

<span data-ttu-id="e9fa6-119">Per usare l' :::no-loc(Razor)::: SDK per compilare librerie di classi contenenti :::no-loc(Razor)::: viste o :::no-loc(Razor)::: pagine, è consigliabile iniziare con il :::no-loc(Razor)::: modello di progetto libreria di classi (RCL).</span><span class="sxs-lookup"><span data-stu-id="e9fa6-119">To use the :::no-loc(Razor)::: SDK to build class libraries containing :::no-loc(Razor)::: views or :::no-loc(Razor)::: Pages, we recommend starting with the :::no-loc(Razor)::: class library (RCL) project template.</span></span> <span data-ttu-id="e9fa6-120">Un RCL usato per compilare :::no-loc(Blazor)::: i file ( *Razor* ) richiede almeno un riferimento al pacchetto [Microsoft. AspNetCore. Components](https://www.nuget.org/packages/Microsoft.AspNetCore.Components) .</span><span class="sxs-lookup"><span data-stu-id="e9fa6-120">An RCL that's used to build :::no-loc(Blazor)::: ( *.razor* ) files minimally requires a reference to the [Microsoft.AspNetCore.Components](https://www.nuget.org/packages/Microsoft.AspNetCore.Components) package.</span></span> <span data-ttu-id="e9fa6-121">Un RCL usato per compilare :::no-loc(Razor)::: viste o pagine (file con *estensione cshtml* ) richiede almeno `netcoreapp3.0` la destinazione o una versione successiva e presenta un `FrameworkReference` al [metapacchetto Microsoft. AspNetCore. app](xref:fundamentals/metapackage-app) nel file di progetto.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-121">An RCL that's used to build :::no-loc(Razor)::: views or pages ( *.cshtml* files) minimally requires targeting `netcoreapp3.0` or later and has a `FrameworkReference` to the [Microsoft.AspNetCore.App metapackage](xref:fundamentals/metapackage-app) in its project file.</span></span>

::: moniker-end

::: moniker range="< aspnetcore-3.0"

<span data-ttu-id="e9fa6-122">Per utilizzare l' :::no-loc(Razor)::: SDK per compilare librerie di classi contenenti :::no-loc(Razor)::: viste o :::no-loc(Razor)::: pagine:</span><span class="sxs-lookup"><span data-stu-id="e9fa6-122">To use the :::no-loc(Razor)::: SDK to build class libraries containing :::no-loc(Razor)::: views or :::no-loc(Razor)::: Pages:</span></span>

* <span data-ttu-id="e9fa6-123">Usare `Microsoft.NET.Sdk.:::no-loc(Razor):::` anziché `Microsoft.NET.Sdk`:</span><span class="sxs-lookup"><span data-stu-id="e9fa6-123">Use `Microsoft.NET.Sdk.:::no-loc(Razor):::` instead of `Microsoft.NET.Sdk`:</span></span>

  ```xml
  <Project SDK="Microsoft.NET.Sdk.:::no-loc(Razor):::">
    <!-- omitted for brevity -->
  </Project>
  ```

* <span data-ttu-id="e9fa6-124">In genere, per `Microsoft.AspNetCore.Mvc` ricevere dipendenze aggiuntive necessarie per compilare e compilare :::no-loc(Razor)::: pagine e visualizzazioni è necessario un riferimento a un pacchetto a :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="e9fa6-124">Typically, a package reference to `Microsoft.AspNetCore.Mvc` is required to receive additional dependencies that are required to build and compile :::no-loc(Razor)::: Pages and :::no-loc(Razor)::: views.</span></span> <span data-ttu-id="e9fa6-125">Come minimo, il progetto deve aggiungere riferimenti a pacchetti:</span><span class="sxs-lookup"><span data-stu-id="e9fa6-125">At a minimum, your project should add package references to:</span></span>

  * `Microsoft.AspNetCore.:::no-loc(Razor):::.Design`
  * `Microsoft.AspNetCore.Mvc.:::no-loc(Razor):::.Extensions`
  * `Microsoft.AspNetCore.Mvc.:::no-loc(Razor):::`

  <span data-ttu-id="e9fa6-126">Il `Microsoft.AspNetCore.:::no-loc(Razor):::.Design` pacchetto fornisce le :::no-loc(Razor)::: attività e le destinazioni di compilazione per il progetto.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-126">The `Microsoft.AspNetCore.:::no-loc(Razor):::.Design` package provides the :::no-loc(Razor)::: compilation tasks and targets for the project.</span></span>

  <span data-ttu-id="e9fa6-127">I pacchetti precedenti sono inclusi in `Microsoft.AspNetCore.Mvc`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-127">The preceding packages are included in `Microsoft.AspNetCore.Mvc`.</span></span> <span data-ttu-id="e9fa6-128">Il markup seguente illustra un file di progetto che usa l' :::no-loc(Razor)::: SDK per compilare :::no-loc(Razor)::: i file per un'app di ASP.NET Core :::no-loc(Razor)::: pages:</span><span class="sxs-lookup"><span data-stu-id="e9fa6-128">The following markup shows a project file that uses the :::no-loc(Razor)::: SDK to build :::no-loc(Razor)::: files for an ASP.NET Core :::no-loc(Razor)::: Pages app:</span></span>

  [!code-xml[](sdk/sample/:::no-loc(Razor):::SDK.csproj)]

::: moniker-end

::: moniker range="= aspnetcore-2.1"

> [!WARNING]
> <span data-ttu-id="e9fa6-129">I `Microsoft.AspNetCore.:::no-loc(Razor):::.Design` `Microsoft.AspNetCore.Mvc.:::no-loc(Razor):::.Extensions` pacchetti e sono inclusi nel [metapacchetto Microsoft. AspNetCore. app](xref:fundamentals/metapackage-app).</span><span class="sxs-lookup"><span data-stu-id="e9fa6-129">The `Microsoft.AspNetCore.:::no-loc(Razor):::.Design` and `Microsoft.AspNetCore.Mvc.:::no-loc(Razor):::.Extensions` packages are included in the [Microsoft.AspNetCore.App metapackage](xref:fundamentals/metapackage-app).</span></span> <span data-ttu-id="e9fa6-130">Tuttavia, il riferimento al pacchetto senza versione `Microsoft.AspNetCore.App` fornisce un metapacchetto all'app che non include la versione più recente di `Microsoft.AspNetCore.:::no-loc(Razor):::.Design` .</span><span class="sxs-lookup"><span data-stu-id="e9fa6-130">However, the version-less `Microsoft.AspNetCore.App` package reference provides a metapackage to the app that doesn't include the latest version of `Microsoft.AspNetCore.:::no-loc(Razor):::.Design`.</span></span> <span data-ttu-id="e9fa6-131">I progetti devono fare riferimento a una versione coerente di `Microsoft.AspNetCore.:::no-loc(Razor):::.Design` (o `Microsoft.AspNetCore.Mvc` ) in modo da includere le correzioni più recenti della fase di compilazione per :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="e9fa6-131">Projects must reference a consistent version of `Microsoft.AspNetCore.:::no-loc(Razor):::.Design` (or `Microsoft.AspNetCore.Mvc`) so that the latest build-time fixes for :::no-loc(Razor)::: are included.</span></span> <span data-ttu-id="e9fa6-132">Per altre informazioni, vedere [questo problema di GitHub](https://github.com/aspnet/:::no-loc(Razor):::/issues/2553).</span><span class="sxs-lookup"><span data-stu-id="e9fa6-132">For more information, see [this GitHub issue](https://github.com/aspnet/:::no-loc(Razor):::/issues/2553).</span></span>

::: moniker-end

### <a name="properties"></a><span data-ttu-id="e9fa6-133">Proprietà</span><span class="sxs-lookup"><span data-stu-id="e9fa6-133">Properties</span></span>

<span data-ttu-id="e9fa6-134">Le proprietà seguenti controllano il :::no-loc(Razor)::: comportamento dell'SDK come parte della compilazione di un progetto:</span><span class="sxs-lookup"><span data-stu-id="e9fa6-134">The following properties control the :::no-loc(Razor):::'s SDK behavior as part of a project build:</span></span>

* <span data-ttu-id="e9fa6-135">`:::no-loc(Razor):::CompileOnBuild`: Quando `true` , compila ed emette l' :::no-loc(Razor)::: assembly come parte della compilazione del progetto.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-135">`:::no-loc(Razor):::CompileOnBuild`: When `true`, compiles and emits the :::no-loc(Razor)::: assembly as part of building the project.</span></span> <span data-ttu-id="e9fa6-136">Il valore predefinito è `true`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-136">Defaults to `true`.</span></span>
* <span data-ttu-id="e9fa6-137">`:::no-loc(Razor):::CompileOnPublish`: Quando `true` , compila ed emette l' :::no-loc(Razor)::: assembly come parte della pubblicazione del progetto.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-137">`:::no-loc(Razor):::CompileOnPublish`: When `true`, compiles and emits the :::no-loc(Razor)::: assembly as part of publishing the project.</span></span> <span data-ttu-id="e9fa6-138">Il valore predefinito è `true`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-138">Defaults to `true`.</span></span>

<span data-ttu-id="e9fa6-139">Le proprietà e gli elementi nella tabella seguente vengono usati per configurare gli input e l'output per l' :::no-loc(Razor)::: SDK.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-139">The properties and items in the following table are used to configure inputs and output to the :::no-loc(Razor)::: SDK.</span></span>

::: moniker range=">= aspnetcore-3.0"

> [!WARNING]
> <span data-ttu-id="e9fa6-140">A partire da ASP.NET Core 3,0, le viste o :::no-loc(Razor)::: le pagine MVC non vengono gestite per impostazione predefinita se le `:::no-loc(Razor):::CompileOnBuild` `:::no-loc(Razor):::CompileOnPublish` proprietà o MSBuild nel file di progetto sono disabilitate.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-140">Starting with ASP.NET Core 3.0, MVC Views or :::no-loc(Razor)::: Pages aren't served by default if the `:::no-loc(Razor):::CompileOnBuild` or `:::no-loc(Razor):::CompileOnPublish` MSBuild properties in the project file are disabled.</span></span> <span data-ttu-id="e9fa6-141">Le applicazioni devono aggiungere un riferimento esplicito a [Microsoft. AspNetCore. Mvc. :::no-loc(Razor)::: Pacchetto RuntimeCompilation](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.:::no-loc(Razor):::.RuntimeCompilation) se l'app si basa sulla compilazione di runtime per elaborare i file *. cshtml* .</span><span class="sxs-lookup"><span data-stu-id="e9fa6-141">Applications must add an explicit reference to the [Microsoft.AspNetCore.Mvc.:::no-loc(Razor):::.RuntimeCompilation](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.:::no-loc(Razor):::.RuntimeCompilation) package if the app relies on runtime compilation to process *.cshtml* files.</span></span>

::: moniker-end

| <span data-ttu-id="e9fa6-142">Items</span><span class="sxs-lookup"><span data-stu-id="e9fa6-142">Items</span></span> | <span data-ttu-id="e9fa6-143">Descrizione</span><span class="sxs-lookup"><span data-stu-id="e9fa6-143">Description</span></span> |
| ----- | ----------- |
| `:::no-loc(Razor):::Generate` | <span data-ttu-id="e9fa6-144">Elementi Item (file con *estensione cshtml* ) che sono input per la generazione di codice.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-144">Item elements ( *.cshtml* files) that are inputs to code generation.</span></span> |
| `:::no-loc(Razor):::Component` | <span data-ttu-id="e9fa6-145">Elementi elemento (file *Razor* ) che sono input per la :::no-loc(Razor)::: generazione di codice componente.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-145">Item elements ( *.razor* files) that are inputs to :::no-loc(Razor)::: component code generation.</span></span> |
| `:::no-loc(Razor):::Compile` | <span data-ttu-id="e9fa6-146">Elementi Item (file con *estensione cs* ) che sono input per le :::no-loc(Razor)::: destinazioni di compilazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-146">Item elements ( *.cs* files) that are inputs to :::no-loc(Razor)::: compilation targets.</span></span> <span data-ttu-id="e9fa6-147">Usare questa `ItemGroup` impostazione per specificare i file aggiuntivi da compilare nell' :::no-loc(Razor)::: assembly.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-147">Use this `ItemGroup` to specify additional files to be compiled into the :::no-loc(Razor)::: assembly.</span></span> |
| `:::no-loc(Razor):::TargetAssemblyAttribute` | <span data-ttu-id="e9fa6-148">Elementi Item utilizzati per generare gli attributi per l' :::no-loc(Razor)::: assembly.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-148">Item elements used to code generate attributes for the :::no-loc(Razor)::: assembly.</span></span> <span data-ttu-id="e9fa6-149">Ad esempio:</span><span class="sxs-lookup"><span data-stu-id="e9fa6-149">For example:</span></span>  <br>`:::no-loc(Razor):::AssemblyAttribute`<br>`Include="System.Reflection.AssemblyMetadataAttribute"`<br>`_Parameter1="BuildSource" _Parameter2="https://docs.microsoft.com/">` |
| `:::no-loc(Razor):::EmbeddedResource` | <span data-ttu-id="e9fa6-150">Elementi elemento aggiunti come risorse incorporate all' :::no-loc(Razor)::: assembly generato.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-150">Item elements added as embedded resources to the generated :::no-loc(Razor)::: assembly.</span></span> |

::: moniker range=">= aspnetcore-3.0"

| <span data-ttu-id="e9fa6-151">Proprietà</span><span class="sxs-lookup"><span data-stu-id="e9fa6-151">Property</span></span> | <span data-ttu-id="e9fa6-152">Descrizione</span><span class="sxs-lookup"><span data-stu-id="e9fa6-152">Description</span></span> |
| -------- | ----------- |
| `:::no-loc(Razor):::TargetName` | <span data-ttu-id="e9fa6-153">Nome file (senza estensione) dell'assembly prodotto da :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="e9fa6-153">File name (without extension) of the assembly produced by :::no-loc(Razor):::.</span></span> |
| `:::no-loc(Razor):::OutputPath` | <span data-ttu-id="e9fa6-154">:::no-loc(Razor):::Directory di output.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-154">The :::no-loc(Razor)::: output directory.</span></span> |
| `:::no-loc(Razor):::CompileToolset` | <span data-ttu-id="e9fa6-155">Utilizzato per determinare il set di strumenti utilizzato per compilare l' :::no-loc(Razor)::: assembly.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-155">Used to determine the toolset used to build the :::no-loc(Razor)::: assembly.</span></span> <span data-ttu-id="e9fa6-156">I valori validi sono `Implicit`, `:::no-loc(Razor):::SDK` e `PrecompilationTool`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-156">Valid values are `Implicit`, `:::no-loc(Razor):::SDK`, and `PrecompilationTool`.</span></span> |
| [<span data-ttu-id="e9fa6-157">EnableDefaultContentItems</span><span class="sxs-lookup"><span data-stu-id="e9fa6-157">EnableDefaultContentItems</span></span>](https://github.com/aspnet/websdk/blob/rel-2.0.0/src/ProjectSystem/Microsoft.NET.Sdk.Web.ProjectSystem.Targets/netstandard1.0/Microsoft.NET.Sdk.Web.ProjectSystem.targets#L21) | <span data-ttu-id="e9fa6-158">Il valore predefinito è `true`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-158">Default is `true`.</span></span> <span data-ttu-id="e9fa6-159">Quando `true` , include i file *web.config* , *. JSON* e *. cshtml* come contenuto nel progetto.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-159">When `true`, includes *web.config* , *.json* , and *.cshtml* files as content in the project.</span></span> <span data-ttu-id="e9fa6-160">Quando viene fatto riferimento tramite `Microsoft.NET.Sdk.Web` , vengono inclusi anche i file in *wwwroot* e i file di configurazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-160">When referenced via `Microsoft.NET.Sdk.Web`, files under *wwwroot* and config files are also included.</span></span> |
| `EnableDefault:::no-loc(Razor):::GenerateItems` | <span data-ttu-id="e9fa6-161">Quando è `true`, include i file *cshtml* degli elementi di `Content` negli elementi di `:::no-loc(Razor):::Generate`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-161">When `true`, includes *.cshtml* files from `Content` items in `:::no-loc(Razor):::Generate` items.</span></span> |
| `Generate:::no-loc(Razor):::TargetAssemblyInfo` | <span data-ttu-id="e9fa6-162">Quando `true` , genera un file con *estensione cs* contenente gli attributi specificati da `:::no-loc(Razor):::AssemblyAttribute` e include il file nell'output di compilazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-162">When `true`, generates a *.cs* file containing attributes specified by `:::no-loc(Razor):::AssemblyAttribute` and includes the file in the compile output.</span></span> |
| `EnableDefault:::no-loc(Razor):::TargetAssemblyInfoAttributes` | <span data-ttu-id="e9fa6-163">Quando è `true`, aggiunge un set predefinito di attributi degli assembly a `:::no-loc(Razor):::AssemblyAttribute`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-163">When `true`, adds a default set of assembly attributes to `:::no-loc(Razor):::AssemblyAttribute`.</span></span> |
| `Copy:::no-loc(Razor):::GenerateFilesToPublishDirectory` | <span data-ttu-id="e9fa6-164">Quando `true` , copia `:::no-loc(Razor):::Generate` gli elementi (file con *estensione cshtml* ) nella directory di pubblicazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-164">When `true`, copies `:::no-loc(Razor):::Generate` items ( *.cshtml* ) files to the publish directory.</span></span> <span data-ttu-id="e9fa6-165">In genere, :::no-loc(Razor)::: i file non sono necessari per un'app pubblicata se partecipano alla compilazione in fase di compilazione o di pubblicazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-165">Typically, :::no-loc(Razor)::: files aren't required for a published app if they participate in compilation at build-time or publish-time.</span></span> <span data-ttu-id="e9fa6-166">Il valore predefinito è `false`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-166">Defaults to `false`.</span></span> |
| `PreserveCompilationReferences` | <span data-ttu-id="e9fa6-167">Quando è `true`, copiare gli elementi dell'assembly di riferimento nella directory di pubblicazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-167">When `true`, copy reference assembly items to the publish directory.</span></span> <span data-ttu-id="e9fa6-168">Gli assembly di riferimento non sono in genere necessari per un'app pubblicata se la :::no-loc(Razor)::: compilazione avviene in fase di compilazione o di pubblicazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-168">Typically, reference assemblies aren't required for a published app if :::no-loc(Razor)::: compilation occurs at build-time or publish-time.</span></span> <span data-ttu-id="e9fa6-169">Impostare su `true` se l'app pubblicata richiede la compilazione in fase di esecuzione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-169">Set to `true` if your published app requires runtime compilation.</span></span> <span data-ttu-id="e9fa6-170">Ad esempio, impostare il valore su `true` se l'app modifica i file *cshtml* in fase di esecuzione o usa le visualizzazioni incorporate.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-170">For example, set the value to `true` if the app modifies *.cshtml* files at runtime or uses embedded views.</span></span> <span data-ttu-id="e9fa6-171">Il valore predefinito è `false`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-171">Defaults to `false`.</span></span> |
| `Include:::no-loc(Razor):::ContentInPack` | <span data-ttu-id="e9fa6-172">Se `true` , tutti :::no-loc(Razor)::: gli elementi di contenuto (file con *estensione cshtml* ) sono contrassegnati per l'inclusione nel pacchetto NuGet generato.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-172">When `true`, all :::no-loc(Razor)::: content items ( *.cshtml* files) are marked for inclusion in the generated NuGet package.</span></span> <span data-ttu-id="e9fa6-173">Il valore predefinito è `false`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-173">Defaults to `false`.</span></span> |
| `Embed:::no-loc(Razor):::GenerateSources` | <span data-ttu-id="e9fa6-174">Quando `true` , aggiunge :::no-loc(Razor)::: gli elementi di generazione ( *. cshtml* ) come file incorporati all' :::no-loc(Razor)::: assembly generato.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-174">When `true`, adds :::no-loc(Razor):::Generate ( *.cshtml* ) items as embedded files to the generated :::no-loc(Razor)::: assembly.</span></span> <span data-ttu-id="e9fa6-175">Il valore predefinito è `false`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-175">Defaults to `false`.</span></span> |
| `Use:::no-loc(Razor):::BuildServer` | <span data-ttu-id="e9fa6-176">Quando è `true`, usa un processo del server di compilazione permanente per ripartire il lavoro di generazione del codice.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-176">When `true`, uses a persistent build server process to offload code generation work.</span></span> <span data-ttu-id="e9fa6-177">Valore predefinito è il valore di `UseSharedCompilation`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-177">Defaults to the value of `UseSharedCompilation`.</span></span> |
| `GenerateMvcApplicationPartsAssemblyAttributes` | <span data-ttu-id="e9fa6-178">Quando `true` , l'SDK genera attributi aggiuntivi usati da MVC in fase di esecuzione per eseguire l'individuazione delle parti dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-178">When `true`, the SDK generates additional attributes used by MVC at runtime to perform application part discovery.</span></span> |
| `DefaultWebContentItemExcludes` | <span data-ttu-id="e9fa6-179">Modello glob per gli elementi elemento che devono essere esclusi dal `Content` gruppo di elementi nei progetti destinati a Web o :::no-loc(Razor)::: SDK</span><span class="sxs-lookup"><span data-stu-id="e9fa6-179">A globbing pattern for item elements that are to be excluded from the `Content` item group in projects targeting the Web or :::no-loc(Razor)::: SDK</span></span> |
| `ExcludeConfigFilesFromBuildOutput` | <span data-ttu-id="e9fa6-180">Quando `true` , i file *. config* e *. JSON* non vengono copiati nella directory dell'output di compilazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-180">When `true`, *.config* and *.json* files do not get copied to the build output directory.</span></span> |
| `Add:::no-loc(Razor):::SupportForMvc` | <span data-ttu-id="e9fa6-181">Quando `true` , configura l' :::no-loc(Razor)::: SDK per aggiungere il supporto per la configurazione MVC richiesta quando si compilano applicazioni contenenti visualizzazioni o :::no-loc(Razor)::: pagine MVC.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-181">When `true`, configures the :::no-loc(Razor)::: SDK to add support for the MVC configuration that is required when building applications containing MVC views or :::no-loc(Razor)::: Pages.</span></span> <span data-ttu-id="e9fa6-182">Questa proprietà viene impostata in modo implicito per i progetti .NET Core 3,0 o versioni successive destinate a Web SDK</span><span class="sxs-lookup"><span data-stu-id="e9fa6-182">This property is implicitly set for .NET Core 3.0 or later projects targeting the Web SDK</span></span> |
| `:::no-loc(Razor):::LangVersion` | <span data-ttu-id="e9fa6-183">Versione della lingua di :::no-loc(Razor)::: destinazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-183">The version of the :::no-loc(Razor)::: Language to target.</span></span> |

::: moniker-end

::: moniker range="< aspnetcore-3.0"

| <span data-ttu-id="e9fa6-184">Proprietà</span><span class="sxs-lookup"><span data-stu-id="e9fa6-184">Property</span></span> | <span data-ttu-id="e9fa6-185">Descrizione</span><span class="sxs-lookup"><span data-stu-id="e9fa6-185">Description</span></span> |
| -------- | ----------- |
| `:::no-loc(Razor):::TargetName` | <span data-ttu-id="e9fa6-186">Nome file (senza estensione) dell'assembly prodotto da :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="e9fa6-186">File name (without extension) of the assembly produced by :::no-loc(Razor):::.</span></span> |
| `:::no-loc(Razor):::OutputPath` | <span data-ttu-id="e9fa6-187">:::no-loc(Razor):::Directory di output.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-187">The :::no-loc(Razor)::: output directory.</span></span> |
| `:::no-loc(Razor):::CompileToolset` | <span data-ttu-id="e9fa6-188">Utilizzato per determinare il set di strumenti utilizzato per compilare l' :::no-loc(Razor)::: assembly.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-188">Used to determine the toolset used to build the :::no-loc(Razor)::: assembly.</span></span> <span data-ttu-id="e9fa6-189">I valori validi sono `Implicit`, `:::no-loc(Razor):::SDK` e `PrecompilationTool`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-189">Valid values are `Implicit`, `:::no-loc(Razor):::SDK`, and `PrecompilationTool`.</span></span> |
| [<span data-ttu-id="e9fa6-190">EnableDefaultContentItems</span><span class="sxs-lookup"><span data-stu-id="e9fa6-190">EnableDefaultContentItems</span></span>](https://github.com/aspnet/websdk/blob/rel-2.0.0/src/ProjectSystem/Microsoft.NET.Sdk.Web.ProjectSystem.Targets/netstandard1.0/Microsoft.NET.Sdk.Web.ProjectSystem.targets#L21) | <span data-ttu-id="e9fa6-191">Il valore predefinito è `true`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-191">Default is `true`.</span></span> <span data-ttu-id="e9fa6-192">Quando `true` , include i file *web.config* , *. JSON* e *. cshtml* come contenuto nel progetto.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-192">When `true`, includes *web.config* , *.json* , and *.cshtml* files as content in the project.</span></span> <span data-ttu-id="e9fa6-193">Quando viene fatto riferimento tramite `Microsoft.NET.Sdk.Web` , vengono inclusi anche i file in *wwwroot* e i file di configurazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-193">When referenced via `Microsoft.NET.Sdk.Web`, files under *wwwroot* and config files are also included.</span></span> |
| `EnableDefault:::no-loc(Razor):::GenerateItems` | <span data-ttu-id="e9fa6-194">Quando è `true`, include i file *cshtml* degli elementi di `Content` negli elementi di `:::no-loc(Razor):::Generate`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-194">When `true`, includes *.cshtml* files from `Content` items in `:::no-loc(Razor):::Generate` items.</span></span> |
| `Generate:::no-loc(Razor):::TargetAssemblyInfo` | <span data-ttu-id="e9fa6-195">Quando `true` , genera un file con *estensione cs* contenente gli attributi specificati da `:::no-loc(Razor):::AssemblyAttribute` e include il file nell'output di compilazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-195">When `true`, generates a *.cs* file containing attributes specified by `:::no-loc(Razor):::AssemblyAttribute` and includes the file in the compile output.</span></span> |
| `EnableDefault:::no-loc(Razor):::TargetAssemblyInfoAttributes` | <span data-ttu-id="e9fa6-196">Quando è `true`, aggiunge un set predefinito di attributi degli assembly a `:::no-loc(Razor):::AssemblyAttribute`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-196">When `true`, adds a default set of assembly attributes to `:::no-loc(Razor):::AssemblyAttribute`.</span></span> |
| `Copy:::no-loc(Razor):::GenerateFilesToPublishDirectory` | <span data-ttu-id="e9fa6-197">Quando `true` , copia `:::no-loc(Razor):::Generate` gli elementi (file con *estensione cshtml* ) nella directory di pubblicazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-197">When `true`, copies `:::no-loc(Razor):::Generate` items ( *.cshtml* ) files to the publish directory.</span></span> <span data-ttu-id="e9fa6-198">In genere, :::no-loc(Razor)::: i file non sono necessari per un'app pubblicata se partecipano alla compilazione in fase di compilazione o di pubblicazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-198">Typically, :::no-loc(Razor)::: files aren't required for a published app if they participate in compilation at build-time or publish-time.</span></span> <span data-ttu-id="e9fa6-199">Il valore predefinito è `false`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-199">Defaults to `false`.</span></span> |
| `CopyRefAssembliesToPublishDirectory` | <span data-ttu-id="e9fa6-200">Quando è `true`, copiare gli elementi dell'assembly di riferimento nella directory di pubblicazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-200">When `true`, copy reference assembly items to the publish directory.</span></span> <span data-ttu-id="e9fa6-201">Gli assembly di riferimento non sono in genere necessari per un'app pubblicata se la :::no-loc(Razor)::: compilazione avviene in fase di compilazione o di pubblicazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-201">Typically, reference assemblies aren't required for a published app if :::no-loc(Razor)::: compilation occurs at build-time or publish-time.</span></span> <span data-ttu-id="e9fa6-202">Impostare su `true` se l'app pubblicata richiede la compilazione in fase di esecuzione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-202">Set to `true` if your published app requires runtime compilation.</span></span> <span data-ttu-id="e9fa6-203">Ad esempio, impostare il valore su `true` se l'app modifica i file *cshtml* in fase di esecuzione o usa le visualizzazioni incorporate.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-203">For example, set the value to `true` if the app modifies *.cshtml* files at runtime or uses embedded views.</span></span> <span data-ttu-id="e9fa6-204">Il valore predefinito è `false`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-204">Defaults to `false`.</span></span> |
| `Include:::no-loc(Razor):::ContentInPack` | <span data-ttu-id="e9fa6-205">Se `true` , tutti :::no-loc(Razor)::: gli elementi di contenuto (file con *estensione cshtml* ) sono contrassegnati per l'inclusione nel pacchetto NuGet generato.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-205">When `true`, all :::no-loc(Razor)::: content items ( *.cshtml* files) are marked for inclusion in the generated NuGet package.</span></span> <span data-ttu-id="e9fa6-206">Il valore predefinito è `false`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-206">Defaults to `false`.</span></span> |
| `Embed:::no-loc(Razor):::GenerateSources` | <span data-ttu-id="e9fa6-207">Quando `true` , aggiunge :::no-loc(Razor)::: gli elementi di generazione ( *. cshtml* ) come file incorporati all' :::no-loc(Razor)::: assembly generato.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-207">When `true`, adds :::no-loc(Razor):::Generate ( *.cshtml* ) items as embedded files to the generated :::no-loc(Razor)::: assembly.</span></span> <span data-ttu-id="e9fa6-208">Il valore predefinito è `false`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-208">Defaults to `false`.</span></span> |
| `Use:::no-loc(Razor):::BuildServer` | <span data-ttu-id="e9fa6-209">Quando è `true`, usa un processo del server di compilazione permanente per ripartire il lavoro di generazione del codice.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-209">When `true`, uses a persistent build server process to offload code generation work.</span></span> <span data-ttu-id="e9fa6-210">Valore predefinito è il valore di `UseSharedCompilation`.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-210">Defaults to the value of `UseSharedCompilation`.</span></span> |
| `GenerateMvcApplicationPartsAssemblyAttributes` | <span data-ttu-id="e9fa6-211">Quando `true` , l'SDK genera attributi aggiuntivi usati da MVC in fase di esecuzione per eseguire l'individuazione delle parti dell'applicazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-211">When `true`, the SDK generates additional attributes used by MVC at runtime to perform application part discovery.</span></span> |
| `DefaultWebContentItemExcludes` | <span data-ttu-id="e9fa6-212">Modello glob per gli elementi elemento che devono essere esclusi dal `Content` gruppo di elementi nei progetti destinati a Web o :::no-loc(Razor)::: SDK</span><span class="sxs-lookup"><span data-stu-id="e9fa6-212">A globbing pattern for item elements that are to be excluded from the `Content` item group in projects targeting the Web or :::no-loc(Razor)::: SDK</span></span> |
| `ExcludeConfigFilesFromBuildOutput` | <span data-ttu-id="e9fa6-213">Quando `true` , i file *. config* e *. JSON* non vengono copiati nella directory dell'output di compilazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-213">When `true`, *.config* and *.json* files do not get copied to the build output directory.</span></span> |
| `Add:::no-loc(Razor):::SupportForMvc` | <span data-ttu-id="e9fa6-214">Quando `true` , configura l' :::no-loc(Razor)::: SDK per aggiungere il supporto per la configurazione MVC richiesta quando si compilano applicazioni contenenti visualizzazioni o :::no-loc(Razor)::: pagine MVC.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-214">When `true`, configures the :::no-loc(Razor)::: SDK to add support for the MVC configuration that is required when building applications containing MVC views or :::no-loc(Razor)::: Pages.</span></span> <span data-ttu-id="e9fa6-215">Questa proprietà viene impostata in modo implicito per i progetti .NET Core 3,0 o versioni successive destinate a Web SDK</span><span class="sxs-lookup"><span data-stu-id="e9fa6-215">This property is implicitly set for .NET Core 3.0 or later projects targeting the Web SDK</span></span> |
| `:::no-loc(Razor):::LangVersion` | <span data-ttu-id="e9fa6-216">Versione della lingua di :::no-loc(Razor)::: destinazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-216">The version of the :::no-loc(Razor)::: Language to target.</span></span> |

::: moniker-end

<span data-ttu-id="e9fa6-217">Per altre informazioni sulle proprietà, vedere [proprietà di MSBuild](/visualstudio/msbuild/msbuild-properties).</span><span class="sxs-lookup"><span data-stu-id="e9fa6-217">For more information on properties, see [MSBuild properties](/visualstudio/msbuild/msbuild-properties).</span></span>

### <a name="targets"></a><span data-ttu-id="e9fa6-218">Server di destinazione</span><span class="sxs-lookup"><span data-stu-id="e9fa6-218">Targets</span></span>

<span data-ttu-id="e9fa6-219">L' :::no-loc(Razor)::: SDK definisce due destinazioni primarie:</span><span class="sxs-lookup"><span data-stu-id="e9fa6-219">The :::no-loc(Razor)::: SDK defines two primary targets:</span></span>

* <span data-ttu-id="e9fa6-220">`:::no-loc(Razor):::Generate`: Il codice genera file con *estensione cs* da `:::no-loc(Razor):::Generate` elementi elemento.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-220">`:::no-loc(Razor):::Generate`: Code generates *.cs* files from `:::no-loc(Razor):::Generate` item elements.</span></span> <span data-ttu-id="e9fa6-221">Utilizzare la `:::no-loc(Razor):::GenerateDependsOn` proprietà per specificare destinazioni aggiuntive che possono essere eseguite prima o dopo questa destinazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-221">Use the `:::no-loc(Razor):::GenerateDependsOn` property to specify additional targets that can run before or after this target.</span></span>
* <span data-ttu-id="e9fa6-222">`:::no-loc(Razor):::Compile`: Compila i file con *estensione cs* generati in un :::no-loc(Razor)::: assembly.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-222">`:::no-loc(Razor):::Compile`: Compiles generated *.cs* files in to a :::no-loc(Razor)::: assembly.</span></span> <span data-ttu-id="e9fa6-223">Usare `:::no-loc(Razor):::CompileDependsOn` per specificare destinazioni aggiuntive che possono essere eseguite prima o dopo questa destinazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-223">Use the `:::no-loc(Razor):::CompileDependsOn` to specify additional targets that can run before or after this target.</span></span>
* <span data-ttu-id="e9fa6-224">`:::no-loc(Razor):::ComponentGenerate`: Il codice genera file con *estensione cs* per `:::no-loc(Razor):::Component` gli elementi elemento.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-224">`:::no-loc(Razor):::ComponentGenerate`: Code generates *.cs* files for `:::no-loc(Razor):::Component` item elements.</span></span> <span data-ttu-id="e9fa6-225">Utilizzare la `:::no-loc(Razor):::ComponentGenerateDependsOn` proprietà per specificare destinazioni aggiuntive che possono essere eseguite prima o dopo questa destinazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-225">Use the `:::no-loc(Razor):::ComponentGenerateDependsOn` property to specify additional targets that can run before or after this target.</span></span>

### <a name="runtime-compilation-of-no-locrazor-views"></a><span data-ttu-id="e9fa6-226">Compilazione di runtime delle :::no-loc(Razor)::: visualizzazioni</span><span class="sxs-lookup"><span data-stu-id="e9fa6-226">Runtime compilation of :::no-loc(Razor)::: views</span></span>

* <span data-ttu-id="e9fa6-227">Per impostazione predefinita, l' :::no-loc(Razor)::: SDK non pubblica gli assembly di riferimento necessari per eseguire la compilazione del runtime.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-227">By default, the :::no-loc(Razor)::: SDK doesn't publish reference assemblies that are required to perform runtime compilation.</span></span> <span data-ttu-id="e9fa6-228">Vengono pertanto generati errori di compilazione nel caso in cui il modello di applicazione si basi su una compilazione runtime&mdash;ad esempio, l'app usa visualizzazioni incorporate o modifica le visualizzazioni dopo aver pubblicato l'app.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-228">This results in compilation failures when the application model relies on runtime compilation&mdash;for example, the app uses embedded views or changes views after the app is published.</span></span> <span data-ttu-id="e9fa6-229">Impostare `CopyRefAssembliesToPublishDirectory` su `true` per continuare con la pubblicazione degli assembly di riferimento.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-229">Set `CopyRefAssembliesToPublishDirectory` to `true` to continue publishing reference assemblies.</span></span>

* <span data-ttu-id="e9fa6-230">Per un'app Web, verificare che l'app sia destinata all' `Microsoft.NET.Sdk.Web` SDK.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-230">For a web app, ensure your app is targeting the `Microsoft.NET.Sdk.Web` SDK.</span></span>

## <a name="no-locrazor-language-version"></a><span data-ttu-id="e9fa6-231">:::no-loc(Razor)::: versione lingua</span><span class="sxs-lookup"><span data-stu-id="e9fa6-231">:::no-loc(Razor)::: language version</span></span>

<span data-ttu-id="e9fa6-232">Quando la destinazione `Microsoft.NET.Sdk.Web` è SDK, la :::no-loc(Razor)::: versione del linguaggio viene dedotta dalla versione del Framework di destinazione dell'app.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-232">When targeting the `Microsoft.NET.Sdk.Web` SDK, the :::no-loc(Razor)::: language version is inferred from the app's target framework version.</span></span> <span data-ttu-id="e9fa6-233">Per i progetti destinati all' `Microsoft.NET.Sdk.:::no-loc(Razor):::` SDK o nel raro caso in cui l'app richieda una versione della lingua diversa da quella :::no-loc(Razor)::: del valore derivato, è possibile configurare una versione impostando la `<:::no-loc(Razor):::LangVersion>` proprietà nel file di progetto dell'app:</span><span class="sxs-lookup"><span data-stu-id="e9fa6-233">For projects targeting the `Microsoft.NET.Sdk.:::no-loc(Razor):::` SDK or in the rare case that the app requires a different :::no-loc(Razor)::: language version than the inferred value, a version can be configured by setting the `<:::no-loc(Razor):::LangVersion>` property in the app's project file:</span></span>

```xml
<PropertyGroup>
  <:::no-loc(Razor):::LangVersion>{VERSION}</:::no-loc(Razor):::LangVersion>
</PropertyGroup>
```

<span data-ttu-id="e9fa6-234">:::no-loc(Razor):::la versione del linguaggio è strettamente integrata con la versione del runtime per cui è stata creata.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-234">:::no-loc(Razor):::'s language version is tightly integrated with the version of the runtime that it was built for.</span></span> <span data-ttu-id="e9fa6-235">La destinazione di una versione di linguaggio non progettata per il runtime non è supportata e probabilmente genera errori di compilazione.</span><span class="sxs-lookup"><span data-stu-id="e9fa6-235">Targeting a language version that isn't designed for the runtime is unsupported and likely produces build errors.</span></span>

## <a name="additional-resources"></a><span data-ttu-id="e9fa6-236">Risorse aggiuntive</span><span class="sxs-lookup"><span data-stu-id="e9fa6-236">Additional resources</span></span>

* [<span data-ttu-id="e9fa6-237">Aggiunte al formato csproj per .NET Core</span><span class="sxs-lookup"><span data-stu-id="e9fa6-237">Additions to the csproj format for .NET Core</span></span>](/dotnet/core/tools/csproj)
* [<span data-ttu-id="e9fa6-238">Elementi di progetto MSBuild comuni</span><span class="sxs-lookup"><span data-stu-id="e9fa6-238">Common MSBuild project items</span></span>](/visualstudio/msbuild/common-msbuild-project-items)
